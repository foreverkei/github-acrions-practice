yaml

name: practice actions

on:
  push:
    workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: |
          echo "Hello GitHub Actions"
          date
          uname -a
